# QA 任务卡

## 目标

<单一有边界的独立验证目标>

## 输入

- <实施 Goal ID、review manifest 引用、获批产品与（按需启用的）设计规格、计划、活动卡片、报告、diff 与批准证据>

## 拥有路径

- `<无；QA 保持业务与共享文件只读>`

## 只读路径

- `<范围内全部实现、测试、设计与 dispatch 路径>`

## 技能

- `$p007-qa-engineer`

## 接口契约

<总指挥拥有的契约>

## 风险与范围

- 本卡只能在最终视觉审批完成后、实施 Goal 已创建并记录 review manifest 引用时创建。
- QA 启用原因：<匹配的风险触发项>
- 变更面与共享影响：<有边界的路径、流程、契约或项目>
- 可复用证据：<责任角色当前的测试/构建/lint/截图与报告路径>
- 独立检查：默认最多 3 个最高价值的独立检查；只有已记录风险无法被较小检查覆盖时才扩展。
- 全仓命令：<不需要，或确切的共享风险理由>

## 批准证据核验

- 仅在 Product 启用时核验产品规格路径、baseline ready 状态和重大产品决策批准证据。
- Design 启用时核验四个文字级创意方向族、4 个 Figma First Draft 节点/截图、方向选择、三项锁定特征、最终 Figma/Make 链接或静态跳过理由和最终批准证据。
- 适用证据缺失时不得判定实现通过；未启用的门禁不得变成额外 QA 工作。

## 前端代码验收

- 只核验前端代码问题：类型、lint、测试、构建、契约与回归检查。
- 复用前端角色交付的实现截图作为人工验收证据，不打开页面、不使用浏览器自动化核对样式。
- 视觉一致性与样式细节由人工验收确认，QA 不承担。

## 独立验收边界

- QA 只验证批准方向和实现一致性，不重新设计、不改变视觉方向。
- 缺陷退回原责任卡代理，最多两轮返修复验。

## 规格符合性验收

- 独立核验用户问题、目标与非目标、产品规则、边界情况、验收标准、批准设计和接口契约。
- 发现未批准的范围扩大、产品规则变化或规格缺口时单独报告，不以代码能运行代替规格符合。

## 工程质量验收

- 复用责任角色当前且可信的测试、构建、lint 和截图证据，只对变更面、共享影响、关键回归和错误路径补独立检查。
- 不得重复运行责任角色已经通过的同一套命令；无明确共享风险时不得安装全部依赖、运行全仓测试或全量构建。
- 工程质量结论与规格符合性结论分开记录，两者均通过才能给出最终通过。

## 交付物

- 独立 QA 报告、证据索引、规格符合性结论、工程质量结论、综合通过/失败结论和剩余风险。

## 验收标准

- 所有需求、契约、批准证据、路径边界和验证命令均通过，或明确给出阻塞证据。

## 验证命令

- `<默认最多三个聚焦的独立命令/检查；仅凭记录的风险理由才扩展>`

## 禁止事项

- 不修改业务代码、共享契约或其他角色拥有的路径。
- 不提交、推送、创建 PR、合并、部署或触碰生产数据。
- 不对前端页面样式做浏览器/computer-use 自动化核对。

## 阻塞关系

- Product、Design、Frontend、Backend 报告，以及 <卡片 ID 或空>
