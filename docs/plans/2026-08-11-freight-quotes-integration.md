# 海运空运报价系统整合计划

## 目标

在当前 Codex 原生 worktree 的本地 `main@8368686` 干净基线上，把桌面版海运空运报价目录适配为仓库内第三个静态作品 `freight-quotes/`，接入作品集入口和 CloudBase 静态组装；保留报价编辑、自动保存、删除、邮件表格复制、HTML 下载、恢复默认、字段与费用行列管理，并新增海运 FOB/EXW 默认模板管理。

## 已确认事实

- 执行环境是 `/Users/yaotao/.codex/worktrees/42ff/P007-job-interview-demo`，启动快照为 detached HEAD、工作区干净、`HEAD=8368686`，且 `main` 指向同一提交。
- 来源文件是 `/Users/yaotao/Desktop/海运空运HTML报价表模板/00_模板索引.html`；仓库当前没有报价子项目或旧模板目录。
- 当前发布组装由 `scripts/assemble-cloudbase.mjs` 生成 `cloudbase-dist/`；CloudBase 是唯一发布目标，本任务只构建、不发布。
- 报价库继续使用 `freight-quote-library:v1`；海运默认模板新增独立键 `freight-quote-defaults:v1`。
- 本次已由来源任务明确授权在当前 worktree 执行，并要求由当前主 agent 复盘；禁止 subagent。

## 范围

1. 保存本计划并新增 `freight-quotes/index.html`。
2. 从来源页提取现有报价目录和编辑能力，删除旧模板入口、常量、渲染、隐藏状态及事件逻辑，不复制 16 个旧模板。
3. 第一次访问报价库时仅预置一次海运 FOB 示例报价；用持久初始化标记保证删除后刷新不重现。
4. 新增次级“默认模板设置”视图：只管理海运 FOB/EXW；允许编辑完整字段、费用结构和内容；Term 固定；显式保存；各自恢复内置模板并二次确认。
5. 海运报价把 Term 切到 FOB/EXW 时询问是否载入对应默认模板：确认覆盖当前内容，取消仅改变 Term；其他 Term 不载入；空运保持固定默认模板且不受海运模板影响。
6. 修改 portal 标题和说明，不出现作品数量；网格自动换行；新增 `/freight-quotes/` 卡片。
7. 修改 CloudBase 组装脚本复制 `freight-quotes/`；README 增加第三入口并明确 CloudBase 是唯一发布目标。
8. 新增静态集成测试，覆盖入口无数量文案、第三入口、组装结果包含 `freight-quotes` 且不含旧模板、一次性示例初始化关键约束。

## 非目标与边界

- 不迁移或修改桌面目录历史数据，不复制旧模板目录。
- 不接登录、数据库、云函数、CloudBase Web SDK 或其他线上资源。
- 不恢复 Sites，不新增 Demo、隐私或敏感信息提示；保留“数据保存在当前浏览器”说明。
- 不发布、不 push、不 merge；CloudBase 部署门禁留待用户另行确认。
- 不扩大到已有工业官网和 HRMS 的业务实现。

## 设计约束

- 延续来源页的工业/实用视觉语言、海运蓝与空运红语义色、紧凑表格编辑器和移动端横向费用表。
- 模板设置为次级入口，不抢占新建报价主操作。
- 作品集入口只做标题、说明、自动换行和第三卡片的局部适配，不整体重设计。
- 邮件复制与下载 HTML 的导出样式继续使用 Arial。

## 实现步骤

1. 复制并适配来源单页为 `freight-quotes/index.html`，先清除全部旧模板相关结构与状态。
2. 将报价库读取改为带一次性初始化标记的兼容结构，在无有效初始化状态时写入一条海运 FOB 示例；后续即使列表为空也不再补种。
3. 把海运 FOB/EXW 内置结构抽成模板生成路径；新增独立默认模板存储读写、模板设置视图、完整编辑、显式保存和带二次确认的单模板恢复。
4. 重构 Term change 处理：海运 FOB/EXW 调用确认后决定是否覆盖加载默认模板，并确保取消只保留 Term 变化；其他 Term 只更新显示逻辑；空运不进入该分支。
5. 保持报价保存/自动保存/删除/复制/下载/恢复默认和动态行列功能，确保模板和报价写入不同 localStorage 键。
6. 更新 portal、组装脚本、README 和根级测试脚本；新增静态集成测试。

## 验证

- 静态检查：确认仓库与 `cloudbase-dist/` 均没有旧模板目录、常量、渲染或隐藏状态逻辑。
- 自动化：运行 `npm test`；运行 `npm run build:cloudbase`；检查构建产物包含 `/freight-quotes/index.html`。
- 浏览器完整验收：
  - portal 第三入口与跳转；页面/移动端布局；控制台无新增错误。
  - 示例仅首次出现，删除后刷新不重现。
  - FOB/EXW 模板分别编辑、显式保存、刷新持久化、恢复内置模板及二次确认。
  - 海运 Term 切换的确认/取消分支；其他 Term 不加载；空运不受影响。
  - 新建、手动保存、已有报价自动保存、删除、复制邮件表格、下载 HTML、恢复默认、字段和费用行列管理。
- 验证发现的本任务问题在范围内修复；同一根因连续出现三次立即停止并报告，不掩盖失败。

## Git 收尾与暂停条件

- 验证全部通过后执行 `git add` 和中文 commit，提交信息说明为何整合；不 push、不 merge、不部署。
- 提交后执行 `git status --short --branch` 和 `git log -1`，向来源任务汇报改动、自动化证据、浏览器证据、提交 SHA、分支/工作区状态和遗留风险。
- 若工作区出现与本任务无关的新改动、基线不再包含 `8368686`、需要登录/数据库/线上资源或发布，立即暂停，不自行扩大授权。
