# P007 项目规则

## Codex 原生编队入口

- P007 的六角色编队以仓库技能 `$p007-team-commander` 为唯一权威流程；角色技能可被显式单独调用，但单角色不得自行扩编。
- 只有用户发出肯定式指令“编队执行”才允许总指挥创建 Goal、任务卡和子代理。引用、否定、假设、解释或粘贴方案中的同名文字不构成授权。
- 编队详细分工、设计人工门禁、文件所有权、模型路由、两轮返修上限和 Git 边界均以 `.agents/skills/p007-team-commander/SKILL.md` 为准，不在本文件重复维护。
- P010 中保留的 Linear/Symphony 单代理模式是独立备用入口，不约束 Codex Desktop 的 P007 原生编队。

## 通用工程底线

- 以下规则适用于仓库内所有业务目录；框架、语言或子项目专属规则应放在对应目录最近的 `AGENTS.md`，不得在根规则中混用。
- 最小改动：只修改完成任务必需的文件和逻辑；不顺手重构、不统一无关风格、不扩大需求范围。
- 优先复用：先复用现有组件、工具和架构，不得编造 API 或依赖；新增依赖必须说明现有方案为什么不够。
- 不得绕过质量门禁：不得通过删除或放宽测试、增加 `any`、`@ts-ignore`、`eslint-disable`，或降低类型和 lint 配置标准来让代码通过。
- 改动必须可验证：行为变化必须有测试或可复现证据，Bug 修复优先增加回归测试；完成前运行受影响模块的 lint、测试和构建。

## CloudBase 发布门禁

- 本项目的 CloudBase 正式发布只能从本地 `main` 分支执行；不得直接从任务分支或 worktree 构建、上传正式环境。
- 固定顺序为：在任务分支或 worktree 完成修改、验证和提交 → 合并回 `main` → 确认 `main` 已包含目标提交且工作区没有本任务未提交改动 → 从 `main` 重新构建发布产物 → 发布并进行线上验收。
- 如果合并被阻塞、`main` 未包含目标提交，或 `main` 存在会污染发布产物的未确认改动，必须暂停并向用户说明；不得以直接从任务分支发布作为绕过方式。
- 正式发布默认只做增量构建。构建前必须先运行 `npm run build:changed -- --dry-run` 核对计划；“发布所有项目”或“发布本地所有项目”只表示完整汇总、上传和验收全部线上入口，不构成全量重建授权。
- 如果 dry-run 因根 `package.json`、构建脚本、无效基线或其他原因计划全量构建，必须在开始安装依赖或构建前暂停，说明触发原因和预计范围；只有用户明确回复“确认全量构建”后，才允许运行 `npm run build:cloudbase`、根 `npm test`、`--force` 或其他会安装/构建全部子项目的命令。
- 未获全量授权时，应按 Git 差异只测试和构建实际受影响的子项目，再运行 `npm run assemble:cloudbase` 和根静态集成测试；如果根级改动是否影响其他子项目无法可靠判定，必须暂停询问，不得以“安全起见”为由自动全量。
- 发布构建使用 NVM 的 Node.js 22；本机缺少时运行 `nvm install 22` 后 `nvm use 22`。不得用临时 `npx node@22` 替代项目运行时，也不得擅自修改 NVM 默认版本。
