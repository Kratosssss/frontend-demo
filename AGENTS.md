# P007 项目规则

## CloudBase 发布门禁

- 本项目的 CloudBase 正式发布只能从本地 `main` 分支执行；不得直接从任务分支或 worktree 构建、上传正式环境。
- 固定顺序为：在任务分支或 worktree 完成修改、验证和提交 → 合并回 `main` → 确认 `main` 已包含目标提交且工作区没有本任务未提交改动 → 从 `main` 重新构建发布产物 → 发布并进行线上验收。
- 如果合并被阻塞、`main` 未包含目标提交，或 `main` 存在会污染发布产物的未确认改动，必须暂停并向用户说明；不得以直接从任务分支发布作为绕过方式。
